<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@drawable/header_background"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="start|center_vertical"
            android:src="@drawable/ic_arrow_back"
            android:padding="6dp" />

        <TextView
            android:id="@+id/tvDoctorHeaderName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="56dp"
            android:background="@drawable/bg_header_doctor_name"
            android:padding="8dp"
            android:text="DR Name"
            android:textColor="#E91E63"
            android:textSize="20sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/btnSettings"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_menu_preferences"
                android:tint="#000000"
                android:padding="6dp"/>

            <ImageView
                android:id="@+id/btnLogout"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@android:drawable/ic_lock_power_off"
                android:tint="#000000"
                android:padding="6dp" />
        </LinearLayout>
    </FrameLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="72dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Doctor card -->
            <LinearLayout
                android:id="@+id/doctorCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="@drawable/bg_card_blue"
                android:padding="12dp"
                android:elevation="2dp"
                android:gravity="center_vertical">

                <!-- UPDATED PROFILE PIC -->
                <ImageView
                    android:id="@+id/doctorProfilePic"
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:background="@drawable/circle_pink"
                    android:padding="10dp"
                    android:src="@drawable/ic_user_black"
                    android:scaleType="centerInside" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:paddingStart="12dp">

                    <TextView
                        android:id="@+id/tvDoctorCardName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="DR Name"
                        android:textColor="#E91E63"
                        android:textSize="18sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/tvDoctorCardEmail"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="doctor@example.com"
                        android:textSize="14sp"
                        android:textColor="#555555" />
                </LinearLayout>
            </LinearLayout>

            <!-- My Patients title with PINK people icon -->
            <TextView
                android:id="@+id/tvPatientsTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="My Patients (0)"
                android:textSize="18sp"
                android:textStyle="bold"
                android:drawableStart="@drawable/ic_patients_pink"
                android:drawablePadding="8dp"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="10dp"/>

            <LinearLayout
                android:id="@+id/containerCards"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

        </LinearLayout>
    </ScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
